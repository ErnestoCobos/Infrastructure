cloudflare_account_id = "REPLACE_WITH_CLOUDFLARE_ACCOUNT_ID"

zones = {
  "getdecant.com" = {
    records = {
      # Add DNS records after exporting or auditing the current Cloudflare zone.
    }
  }
}
